# Core - required
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
tzdata>=2024.1 ; platform_system == "Windows"

# Significance tests. Optional: without scipy the tool falls back to exact
# pure-Python implementations that produce the same numbers.
scipy>=1.10

# Camera integration (TP-Link Tapo C100 and other ONVIF cameras). Optional.
requests>=2.28      # ONVIF SOAP over HTTP
opencv-python>=4.8  # RTSP frame grab for snapshots
